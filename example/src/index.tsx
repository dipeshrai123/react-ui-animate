import ReactDOM from 'react-dom/client';

import './index.css';

const App = () => <div>RUN WITH STORYBOOK</div>;

const root = ReactDOM.createRoot(
  document.getElementById('root') as HTMLElement
);

root.render(<App />);
